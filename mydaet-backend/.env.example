# Optional runtime values for local tooling/documentation only.
# Do not commit real secrets.

# Comma-separated list of trusted web origins for trackLookup HTTP CORS.
# Leave empty to allow all origins.
TRACK_LOOKUP_ALLOWED_ORIGINS=
